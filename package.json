{
  "name": "ffmpegaddon",
  "version": "1.0.0",
  "description": "FFmpeg Addon for Node.js",
  "main": "index.js",
  "scripts": {
    "configure": "cmake-js configure",
    "build": "cmake-js rebuild",
    "build:cross": "./scripts/build_ffmpeg_mingw.sh ./buildout-w64",
    "configure:cross": "echo 'Use scripts/build_ffmpeg_mingw.sh to build FFmpeg and then run cmake with CC/CXX set to x86_64-w64-mingw32-*'",
    "test": "node ./test/test.js",
    "parser": "node ./parsers.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cmake-js": "^7.3.0",
    "node-addon-api": "^8.0.0"
  },
  "devDependencies": {
    "node-api-headers": "^1.6.0"
  }
}
